import React from 'react'
import { createContext } from 'react'
import { weatherType } from '../utilities/weatherType'

export const DashboardContext =
  (createContext < bgColor) | (undefined > undefined)
